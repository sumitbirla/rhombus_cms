<h2>
  <div class="pull-right">
	  <%= link_to "new location", new_admin_cms_location_path, class: 'btn btn-sm btn-default' %>
	  <%= link_to "setup attributes", admin_system_attributes_path(type: :location), class: 'btn btn-sm btn-default' %>
	  <%= link_to "setup categories", admin_system_categories_path(type: :location), class: 'btn btn-sm btn-default' %>
  </div>
	<i class="fa fa-map-marker"></i>&nbsp; Locations
</h2>

<%= render "admin/shared/search_results_header", object: @locations %>

<%= will_paginate @locations %>
<table class="table table-striped table-condensed">
	<tr>
		<th>Name & address</th>
		<th>Country</th>
		<th>Phone</th>
		<th>&nbsp;</th>
	</tr>
	<% @locations.each do |loc| %>
	<tr>
		<td><%= link_to loc.name, admin_cms_location_path(loc) %></td>
		<td><%= loc.country %></td>
		<td><%= loc.phone %></td>
		<td>
			<%= link_to "edit", edit_admin_cms_location_path(loc) %> &nbsp;
			<%= link_to "delete", 
						  { action: "destroy", id: loc.id }, 
						  method: :delete, 
						  data: { confirm: "Are you sure?" } %>
		</td>	
	<% end %>
</table>



<% content_for :title do %>
    Locations
<% end %>



<% content_for :breadcrumbs do %>
	<li><a href="#">Web Content</a></li>
	<li class="active">Locations</li>
<% end %>
